# Core
fastapi==0.115.5
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.6.1
python-dotenv==1.1.0
httpx==0.28.1

# OpenAI-compatible client (used for Foundry Local inference)
openai==2.24.0

# Foundry Local SDK
foundry-local-sdk==1.2.0

# MCP (Model Context Protocol)
mcp==1.27.2
fastmcp==2.14.7

# Storage
aiofiles==24.1.0

# OpenTelemetry (local: console exporter; Azure adds Azure Monitor exporter)
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0

# PDF report generation (learner readiness + manager handoff brief)
reportlab>=4.0.0

# Calibrated readiness model (pure numpy — no sklearn)
numpy>=1.26.0

# Utilities
python-multipart==0.0.12
sse-starlette==2.1.3
structlog==24.4.0
tenacity==9.0.0
