# Minimal dependencies for the Foundry Hosted Agent container.
# Deliberately excludes:
#   - azure-ai-evaluation  (eval SDK — run locally, not in the hosted container)
#   - fastmcp / mcp        (MCP server — not needed at hosted agent runtime)
#   - foundry-local-sdk    (local-only inference — not used in cloud)
#   - pyodbc               (Fabric SQL — not needed at hosted agent runtime)

# Web server
fastapi==0.115.5
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.6.1
python-dotenv==1.1.0
httpx==0.28.1
python-multipart==0.0.12
sse-starlette==2.1.3
aiofiles==24.1.0

# OpenAI-compatible client (Responses API calls)
openai==2.24.0

# Azure AI Foundry
azure-ai-projects>=2.1.0
azure-ai-inference==1.0.0b9
azure-identity==1.19.0
azure-search-documents>=11.6.0
azure-keyvault-secrets>=4.8.0
azure-cosmos==4.9.0

# Telemetry
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
azure-monitor-opentelemetry>=1.6.4

# Calibrated readiness model
numpy>=1.26.0

# PDF report generation
reportlab>=4.0.0

# Utilities
structlog==24.4.0
tenacity==9.0.0
